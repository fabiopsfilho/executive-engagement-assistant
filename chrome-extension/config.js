/**
 * Single source of truth for the web app URL the extension embeds.
 *
 * If the Amplify branch/domain changes (e.g. promoting v2-redesign to a
 * production/default branch), update ONLY this value — everything else reads it.
 */
globalThis.TC_ADVISOR_APP_URL = 'https://v2-redesign.d3uctzirlcp4lr.amplifyapp.com';
